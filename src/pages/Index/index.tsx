import { Link } from "@tanstack/react-location";

const Index: React.FC = ({}) => {
  return (
    <div>
      <p>testtest</p>
      <Link to={"service"}>サービスに移動</Link>
    </div>
  );
};

export default Index;
